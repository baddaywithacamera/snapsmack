*.pyc
__pycache__/

# Google SA keys and OAuth tokens — must NEVER be committed.
# Runtime location: C:\SmackUpYourBackup\  (outside the repo)
*-drive-key-*.json
*_token.json
*_readonly_token.json
*_box_token.json

# SFTP host-key pins (SECAUDIT 037) — runtime state, per-machine.
suyb_known_hosts

# Credential vault metadata (SECAUDIT 037) — per-install, never commit.
vault.meta
