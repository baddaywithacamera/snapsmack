# UNZUCKER Linux (Chrome/Blink) port — real pip deps only.
# snap_blink itself is stdlib. These are the tool's existing work deps that the
# port imports through config/ig_parser/poster/exif_writer.
requests>=2.28
Pillow>=10.0
piexif>=1.1.3
keyring>=24.0     # optional — OS credential store (libsecret on Linux); falls back to base64 if absent
# ===== SNAPSMACK EOF =====
