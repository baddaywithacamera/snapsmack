PySide6>=6.6
keyring>=24.0            # OS keychain for the WordPress application password; else the shared vault seals it
requests>=2.31
# The editor, poster, MOSAIC and AI providers are COLD SNAP's (tools/coldsnap) - see tools/coldsnap/requirements.txt.
