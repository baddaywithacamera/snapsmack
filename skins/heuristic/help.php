<?php
/**
 * SNAPSMACK - HEURISTIC contextual help
 *
 * SNAPSMACK_EOF_HEADER
 *     // ===== SNAPSMACK EOF =====
 * Last non-empty line of this file MUST match the line above.
 */

// ARCH-04: converted from a raw-HTML echo (which the help loader could not pick
// up, so this skin's help was never shown) to the flat topic list the loader
// supports. The HTML body is unchanged.
return [
  [
    'title' => 'Heuristic',
    'body'  => <<<'HTML'
<p>I am a HEURISTIC photographic presentation system. I display a conventional
   three-column GRAMOFSMACK grid because needless alarm would interfere with
   the book audit.</p>

<h3>Logic Memory Centre</h3>
<p>The red optical-memory bank brightens while photographs are being served.
   <strong>System Activity</strong> can leave it live, quiet it, or make the
   presentation static.</p>

<h3>Infomatic screens</h3>
<p>A visible photograph may briefly become an infomatic display and then return.
   Classic automatic screens are supplied. Your own screens can be associated
   with a category slug or a specific post.</p>

<p>Enter rules in <strong>Category and Post Screens</strong>, separated by
   semicolons:</p>
<pre>clive-cussler=CUS|CONTAMINATION|fault|total;
larry-niven=NIV|KNOWN SPACE|blue|ordinal;
post:42=COM|PRIORITY MESSAGE|calm|images</pre>

<p>Each rule is <code>target=CODE|LABEL|colour|counter</code>. Colours may be
   <code>calm</code>, <code>fault</code>, <code>blue</code>, or
   <code>violet</code>. Counters may be <code>total</code>,
   <code>ordinal</code>, <code>images</code>, <code>post</code>,
   <code>none</code>, or a short literal value.</p>

<h3>Accessibility and motion</h3>
<p>Infomatics use slow pulses below the three-flashes-per-second threshold.
   They do not run on phones or for visitors who request reduced motion.
   The effects are decorative and hidden from assistive technology; links
   and image alternatives remain ordinary page content.</p>

<p>No cookies are used by this skin. No sound is produced. Dave has been
   advised that all systems are functioning normally.</p>
HTML
  ],
];
// ===== SNAPSMACK EOF =====
