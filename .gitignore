# Live database credentials — never commit this
core/db.php

# Backups contain full source + database dumps — never commit
backups/

# Updater temp files
/tmp/snapsmack_update/

# User uploads (too large, site-specific)
img_uploads/
media_assets/

# Claude / AI tooling
CLAUDE.md

# OS junk
.DS_Store
Thumbs.db
desktop.ini
HIP-TO-BE-SQUARE-SPEC.docx
PICASA-WEB-ALBUMS-SPEC.docx

# Forum server — separate deployable for snapsmack.ca, not part of the install package
forum-server/
