# Live database credentials — never commit this
core/db.php

# Backups contain full source + database dumps — never commit
backups/

# Schema snapshots are install-specific (contain live AUTO_INCREMENT values etc.)
# They have no place in the distribution repo.
database/schema/

# Updater temp files
/tmp/snapsmack_update/

# User uploads (too large, site-specific)
img_uploads/
media_assets/

# Claude / AI tooling
CLAUDE.md

# OS junk
.DS_Store
Thumbs.db
desktop.ini
HIP-TO-BE-SQUARE-SPEC.docx
PICASA-WEB-ALBUMS-SPEC.docx


# Release packages (built by Smack Central, not stored in source)
releases/
